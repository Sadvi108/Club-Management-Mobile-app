// The instructor "Settings" tab reuses the role-aware Profile screen (identity, club
// overview, branches, theme, logout). Same component the student "Profile" tab renders.
export { default } from "./profile";
